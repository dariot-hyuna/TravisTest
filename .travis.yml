language: node_js
cache: yarn
node_js:
  - 'stable'

install:
 - yarn

script:
 - if [ "$TRAVIS_BRANCH" = "staging" ]; then yarn run staging; fi
 - if [ "$TRAVIS_BRANCH" = "prod" ]; then yarn run prod; fi
